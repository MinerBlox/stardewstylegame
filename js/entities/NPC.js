export class NPC{constructor(def){Object.assign(this,def);this.x=1000;this.y=900;this.scene='world';this.target=null;this.dir='south';this.walk=0;this.speed=58;this.pause=0}setScene(scene,x,y){this.scene=scene;this.x=x;this.y=y}moveToward(dt,x,y,blocked){const dx=x-this.x,dy=y-this.y,d=Math.hypot(dx,dy);if(d<4)return true;const vx=dx/d,vy=dy/d;this.dir=Math.abs(vx)>Math.abs(vy)?(vx<0?'west':'east'):(vy<0?'north':'south');const s=Math.min(d,this.speed*dt/1000),nx=this.x+vx*s,ny=this.y+vy*s;if(!blocked(nx,this.y,9))this.x=nx;if(!blocked(this.x,ny,9))this.y=ny;this.walk+=dt*.009;return d<7}}
}
