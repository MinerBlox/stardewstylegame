export class Input{constructor(){this.keys=new Set();this.pressed=new Set();addEventListener('keydown',e=>{if(!this.keys.has(e.key))this.pressed.add(e.key);this.keys.add(e.key);if(['ArrowUp','ArrowDown','ArrowLeft','ArrowRight',' ','Tab'].includes(e.key))e.preventDefault()});addEventListener('keyup',e=>this.keys.delete(e.key));addEventListener('blur',()=>{this.keys.clear();this.pressed.clear()})}down(...ks){return ks.some(k=>this.keys.has(k))}hit(...ks){return ks.some(k=>this.pressed.has(k))}endFrame(){this.pressed.clear()}}
